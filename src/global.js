import { createGlobalStyle } from "styled-components";

// Google Search for docs

export default createGlobalStyle `
    * {
        margin: 0; 
        padding: 0;
        box-sizing: border-box;
     }
`
